# DTM Mock — security.log aylantirish (cheksiz o'sib ketmasin)
# O'rnatish: sudo cp deploy/logrotate-dtm.conf /etc/logrotate.d/dtm-mock
# Yo'lni o'z serveringizga moslang.
/home/muhammadullo/Desktop/dtm_omr/data/security.log {
    weekly
    rotate 8
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
